# Copy to scripts/.env (gitignored). Optional if stay.hr apex A record exists.
TARGET_SERVER_IP=YOUR.HETZNER.PUBLIC.IP

# Optional overrides
# TRAEFIK_ENV=/opt/stacks/traefik/.env
# CLOUDFLARE_ZONE_NAME=stay.hr
# STAY_DNS_HOSTS="api.stay.hr admin.stay.hr"
